# This file has been removed to avoid conflicts with GitHub's default CodeQL setup.
# The repository uses GitHub's default code scanning configuration instead.
# For custom CodeQL configurations, disable the default setup in repository settings.
